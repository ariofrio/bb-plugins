---
"bb-plugin-thread-stages": minor
---

Rename Thread workflow to Thread stages and polish its project filter, stage headers, counts, spacing, hover behavior, and command labels to match BB's built-in sidebar.
